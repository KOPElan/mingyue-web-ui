# JWT Secret (change in production!)
JWT_SECRET=mingyue-secret-change-me

# Server port
PORT=3000

# Default admin credentials (change in production!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Initial agent configuration (optional JSON)
# INITIAL_AGENTS=[{"id":"agent1","name":"Local Agent","address":"http://localhost:8080","apiKey":"your-api-key","status":"online","version":"1.0.0"}]
